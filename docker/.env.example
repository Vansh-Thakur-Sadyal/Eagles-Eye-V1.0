# Copy to docker/.env before running compose.
SENTINEL_JWT_SECRET=generate-a-long-random-string
POSTGRES_PASSWORD=change-me
SENTINEL_ADMIN_PASSWORD=
BACKEND_PORT=8000
FRONTEND_PORT=8080
SENTINEL_CORS_ORIGINS=http://localhost:8080
SENTINEL_DEVICE_MODE=auto
SENTINEL_LLM_PROVIDER=none
