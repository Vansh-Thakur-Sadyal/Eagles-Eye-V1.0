# ---- Core API ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
websockets==14.1
httpx==0.28.1
python-dotenv==1.0.1

# ---- Database ----
SQLAlchemy==2.0.36
alembic==1.14.0
# psycopg[binary]==3.2.3        # uncomment for PostgreSQL

# ---- Auth / security ----
passlib[bcrypt]==1.7.4
PyJWT==2.10.1
email-validator==2.2.0        # pydantic EmailStr for the portal forms

# ---- Vision / ML (install separately, see docs/INSTALL.md) ----
numpy==1.26.4
opencv-python-headless==4.10.0.84
Pillow==11.0.0
scipy==1.14.1
# torch / torchvision  -> install with CUDA 12.8 wheel (RTX 50-series). See docs/INSTALL.md
# ultralytics==8.3.55
# transformers==4.48.0
# accelerate==1.2.1

# ---- RAG / vector ----
chromadb==0.5.23
# sentence-transformers==3.3.1

# ---- Utils ----
orjson==3.10.12
psutil==6.1.1
PyYAML==6.0.2
